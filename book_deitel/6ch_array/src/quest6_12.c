/* a */
int counts[10] = {0};
/* b */
int bonus[15] = {0};
for(int i = 0; i < 15; i++) bonus[i] += 1;
/* c */
double monthlytemperatures[12] = {0};
for(int i = 0; i < 12; i++) scanf("%lf", &monthlytemperatures[i]);
/* d */
for(int i = 0; i < 5; i++) printf("%d\n", bestScores[i]);